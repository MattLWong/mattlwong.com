jQuery(function($) {
    $('.slider').bxSlider({
        adaptiveHeight: true,
        // mode: 'fade'
        // auto: true
    });
    $('.slider-auto').bxSlider({
        adaptiveHeight: true,
        mode: 'fade',
        auto: true
    });
});
